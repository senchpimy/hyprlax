# Test config with relative paths
# These paths should be resolved relative to this config file

# Using relative path to go up and access examples
layer ../examples/mountains/layer0_sky.png 0.3 1.0
layer ../examples/mountains/layer1_far_mountains.png 0.5 0.9

# Test with ./ prefix (current directory relative to config)
# layer ./local_image.png 1.0 0.8